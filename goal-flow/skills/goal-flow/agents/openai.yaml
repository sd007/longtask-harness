interface:
  display_name: "Goal Flow"
  short_description: "Classify tasks and choose a safe harness"
  default_prompt: "Use $goal-flow to classify this task, choose the smallest safe harness, plan, execute, verify, and deliver it reliably."
