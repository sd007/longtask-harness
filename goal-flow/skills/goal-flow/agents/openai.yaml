interface:
  display_name: "Goal Flow"
  short_description: "Keep ordinary tasks lean and escalate safely"
  default_prompt: "Use $goal-flow to choose the smallest safe harness, plan, execute, verify, and escalate only when needed."
