interface:
  display_name: "Goal Flow"
  short_description: "Align visually, implement, and verify lightly"
  default_prompt: "Use $goal-flow to understand and visually align this task when useful, then implement and verify it through the smallest safe three-phase flow."
