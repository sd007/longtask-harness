interface:
  display_name: "Goal Flow"
  short_description: "Align the solution, implement faithfully, verify key scenarios"
  default_prompt: "Use $goal-flow to align a compact solution and evolvability probe, implement it faithfully, then verify the few product scenarios most likely to expose a real defect."
